/*
Nosso exemplo

Aplicação final

Quando o usuário clicar no botão Gerar numero, um número aleatório será exibido no label acima.

O que vamos aprender?

No curso anterior você aprendeu a criar componentes.
 Neste curso daremos um passo à frente e aprenderemos os conceitos:

State Hook

Evento onClick

O que é State Hook?

O State Hook (useState), basicamente nos permite criar uma variável que ao mudar 
de valor, automaticamente, será alterada na tela do usuário.

Por exemplo, se a variável nomeProduto for alterada, precisamos 
que o usuário veja o novo nome do produto - o React permite que isso
 aconteça de forma automática - ou - reativa, como o nome da tecnologia implica.

O que é o evento onClick?

Ao clicar em um botão é comum que algo aconteça. 
Para chamarmos esta ação utilizamos o evento onClick.

Neste exemplo, quando o usuário clicar no botão, o código associado ao evento 
onClick será executado (a geração do número aleatório).

Esse é o conceito da programação de eventos - associamos um código a um evento,
ou trigger. Quando este evento/trigger for disparado pela aplicação (ou pelo usuário)
o código associado será executado.
*/